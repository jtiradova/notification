# Font Awesome Sharp icons (required — set FONTAWESOME_TOKEN in Vercel env)
@fortawesome:registry=https://npm.fontawesome.com
//npm.fontawesome.com/:_authToken=${FONTAWESOME_TOKEN}

# Must match pnpm-lock.yaml settings (Vercel uses pnpm 10 with frozen install)
auto-install-peers=true
